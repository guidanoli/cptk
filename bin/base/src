#!/usr/bin/env bash
#
# Summary: Get source code file path

# Make temporary directory
mkdir -p "bin/langs/$lang/temp"

# Add prefix to path
printf "bin/langs/$lang/"

# Make subcall
bin/subcall "$file" "$@"
