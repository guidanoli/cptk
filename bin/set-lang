#!/usr/bin/env bash
#
# Summary: Sets current language
#
# Usage: bin/set-lang [LANG]
#
# Sets current language as LANG, if present

if [[ $# -eq 1 ]]; then
	echo "$1" > bin/lang.dat
	mkdir -p "bin/langs/$1"
fi
